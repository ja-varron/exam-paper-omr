services:
  - type: web
    name: omr-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api.api_endpoint:app --app-dir src --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
